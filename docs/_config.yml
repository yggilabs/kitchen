name: kitchen
short_name: kitchen
background_color: "#fff"
theme_color: "#fff"
plugins:
  - jekyll-seo-tag
collections:
  recipes:
    output: true
defaults:
  - scope:
      path: ""
      type: recipes
    values:
      layout: default
